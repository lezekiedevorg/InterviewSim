# Mode réunion vocal v1 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transformer la phase `chat` en une visio façon Meet où le recruteur parle à voix haute (voix native du navigateur), le candidat répond au clavier, avec webcam locale optionnelle et transcription.

**Architecture:** La phase `chat` de `app/page.tsx` rend `<MeetingRoom>` (qui affiche d'abord un salon `MeetingLobby`, puis la salle). La voix passe par un hook `useSpeech` autour de `window.speechSynthesis`, alimenté par un helper pur `nextSpeakableChunk` qui découpe le flux du recruteur en phrases. La logique de streaming/état existante ne change pas — seule la présentation change.

**Tech Stack:** Next.js (App Router, client components), TypeScript, Tailwind v3, Web Speech API (`speechSynthesis`), `getUserMedia` (webcam locale), Vitest (logique pure).

## Global Constraints

- **Gratuit d'abord** : AUCUNE nouvelle dépendance npm. Voix = `window.speechSynthesis` natif.
- **Style** : Tailwind, accent émeraude (`brand-*`), composants/typo existants ; français.
- **v1 = sens unique** : le recruteur parle, le candidat répond au CLAVIER. Pas de reconnaissance vocale (reporté v2).
- **Webcam** : `getUserMedia({ video: true })`, 100 % local, jamais envoyée nulle part ; refus/indispo → avatar + message, pas de crash.
- **Ne pas casser** la logique existante de `app/page.tsx` (streaming `streamRecruiter`, débrief, save Supabase) : seule la présentation de la phase `chat` change.
- **Next.js modifié** : lire `node_modules/next/dist/docs/` avant d'écrire du code Next spécifique (ici : client components React classiques, hooks — pas de nouvelle API Next).
- **Tests** : Vitest sur logique pure uniquement ; hooks/composants à API navigateur vérifiés au navigateur (pas d'infra de test de composants).
- Types partagés : `ChatMessage = { role: "recruiter" | "candidate"; text: string }` et `InterviewContext` viennent de `lib/types.ts`.

---

### Task 1: Helper pur `nextSpeakableChunk`

**Files:**
- Create: `lib/speech.ts`
- Test: `tests/speech.test.ts`

**Interfaces:**
- Produces: `nextSpeakableChunk(fullText: string, spokenLen: number): { chunk: string; spokenLen: number }` — renvoie la prochaine phrase complète après `spokenLen` (coupe sur `.!?…` ou `\n`) et le nouvel offset ; si aucune phrase complète n'est disponible, renvoie `{ chunk: "", spokenLen }` inchangé.

- [ ] **Step 1: Write the failing test**

Create `tests/speech.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { nextSpeakableChunk } from "../lib/speech";

describe("nextSpeakableChunk", () => {
  it("extrait la première phrase complète et avance l'offset", () => {
    const r = nextSpeakableChunk("Bonjour. Ca va?", 0);
    expect(r.chunk).toBe("Bonjour.");
    expect(r.spokenLen).toBe(8);
  });

  it("reprend à l'offset sans relire la phrase précédente", () => {
    const r = nextSpeakableChunk("Bonjour. Ca va?", 8);
    expect(r.chunk).toBe("Ca va?");
    expect(r.spokenLen).toBe(15);
  });

  it("ne renvoie rien tant que la phrase est incomplète", () => {
    const r = nextSpeakableChunk("Bonjour sans fin", 0);
    expect(r.chunk).toBe("");
    expect(r.spokenLen).toBe(0);
  });

  it("coupe aussi sur un retour à la ligne", () => {
    const r = nextSpeakableChunk("Ligne un\nLigne deux.", 0);
    expect(r.chunk).toBe("Ligne un");
    expect(r.spokenLen).toBe(9);
  });

  it("gère les points de suspension et exclamations", () => {
    expect(nextSpeakableChunk("Super !", 0).chunk).toBe("Super !");
    expect(nextSpeakableChunk("Attends…", 0).chunk).toBe("Attends…");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/speech.test.ts`
Expected: FAIL — `Cannot find module '../lib/speech'`.

- [ ] **Step 3: Write minimal implementation**

Create `lib/speech.ts`:

```ts
// Découpe un flux de texte en phrases prononçables, une à la fois.
// ponytail: coupe naïvement sur .!?…/newline ; "Node.js" ou "3.14" seront
// scindés (rare à l'oral, tolérable). Upgrade: regex tenant compte des abréviations si gênant.
export function nextSpeakableChunk(
  fullText: string,
  spokenLen: number
): { chunk: string; spokenLen: number } {
  const rest = fullText.slice(spokenLen);
  let boundary = -1;
  for (let i = 0; i < rest.length; i++) {
    const c = rest[i];
    if (c === "." || c === "!" || c === "?" || c === "…" || c === "\n") {
      boundary = i;
      break;
    }
  }
  if (boundary === -1) return { chunk: "", spokenLen };
  const end = spokenLen + boundary + 1;
  return { chunk: fullText.slice(spokenLen, end).trim(), spokenLen: end };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run tests/speech.test.ts`
Expected: PASS (5 tests).

- [ ] **Step 5: Commit**

```bash
git add lib/speech.ts tests/speech.test.ts
git commit -m "feat: helper nextSpeakableChunk (découpe du flux en phrases)"
```

---

### Task 2: Hook `useSpeech`

**Files:**
- Create: `lib/useSpeech.ts`

**Interfaces:**
- Produces: `useSpeech(): { supported: boolean; speak: (text: string) => void; cancel: () => void; muted: boolean; toggleMute: () => void; isSpeaking: boolean }`.

> Pas de test unitaire (API navigateur). Vérification : `npx tsc --noEmit`.

- [ ] **Step 1: Write the hook**

Create `lib/useSpeech.ts`:

```ts
"use client";

import { useCallback, useEffect, useRef, useState } from "react";

export function useSpeech() {
  const [supported, setSupported] = useState(false);
  const [muted, setMuted] = useState(false);
  const [isSpeaking, setIsSpeaking] = useState(false);
  const voiceRef = useRef<SpeechSynthesisVoice | null>(null);
  const mutedRef = useRef(false);

  useEffect(() => {
    if (typeof window === "undefined" || !("speechSynthesis" in window)) return;
    setSupported(true);
    const pickVoice = () => {
      const voices = window.speechSynthesis.getVoices();
      voiceRef.current = voices.find((v) => v.lang.startsWith("fr")) ?? voices[0] ?? null;
    };
    pickVoice();
    window.speechSynthesis.onvoiceschanged = pickVoice;
    return () => {
      window.speechSynthesis.onvoiceschanged = null;
      window.speechSynthesis.cancel();
    };
  }, []);

  const speak = useCallback(
    (text: string) => {
      if (!supported || mutedRef.current || !text.trim()) return;
      const u = new SpeechSynthesisUtterance(text);
      u.lang = "fr-FR";
      if (voiceRef.current) u.voice = voiceRef.current;
      u.onstart = () => setIsSpeaking(true);
      u.onend = () => setIsSpeaking(false);
      window.speechSynthesis.speak(u);
    },
    [supported]
  );

  const cancel = useCallback(() => {
    if (supported) window.speechSynthesis.cancel();
    setIsSpeaking(false);
  }, [supported]);

  const toggleMute = useCallback(() => {
    setMuted((m) => {
      const next = !m;
      mutedRef.current = next;
      if (next && supported) window.speechSynthesis.cancel();
      return next;
    });
  }, [supported]);

  return { supported, speak, cancel, muted, toggleMute, isSpeaking };
}
```

- [ ] **Step 2: Type-check**

Run: `npx tsc --noEmit`
Expected: aucune erreur.

- [ ] **Step 3: Commit**

```bash
git add lib/useSpeech.ts
git commit -m "feat: hook useSpeech (speechSynthesis natif, voix FR, mute)"
```

---

### Task 3: `RecruiterTile` + `TranscriptPanel`

**Files:**
- Create: `app/components/meeting/RecruiterTile.tsx`
- Create: `app/components/meeting/TranscriptPanel.tsx`

**Interfaces:**
- Consumes: `ChatMessage` de `@/lib/types`.
- Produces: `RecruiterTile({ speaking }: { speaking: boolean })` ; `TranscriptPanel({ history }: { history: ChatMessage[] })`.

> Présentationnel. Vérification : `npx tsc --noEmit`.

- [ ] **Step 1: Write `RecruiterTile`**

Create `app/components/meeting/RecruiterTile.tsx`:

```tsx
export function RecruiterTile({ speaking }: { speaking: boolean }) {
  return (
    <div className="relative flex aspect-video w-full items-center justify-center overflow-hidden rounded-2xl bg-gradient-to-br from-slate-800 to-slate-900 text-white shadow-soft">
      {speaking && (
        <span className="absolute inset-0 animate-pulse rounded-2xl ring-4 ring-brand-500/50" aria-hidden />
      )}
      <div className="flex flex-col items-center gap-3">
        <span
          className={`grid h-20 w-20 place-items-center rounded-full bg-brand-600 text-2xl font-bold transition-transform ${
            speaking ? "scale-110" : "scale-100"
          }`}
        >
          RH
        </span>
        <span className="text-sm font-medium text-slate-200">
          Recruteur {speaking && <span className="text-brand-300">· parle…</span>}
        </span>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Write `TranscriptPanel`**

Create `app/components/meeting/TranscriptPanel.tsx`:

```tsx
import type { ChatMessage } from "@/lib/types";

export function TranscriptPanel({ history }: { history: ChatMessage[] }) {
  return (
    <div className="flex max-h-[40vh] flex-col gap-2 overflow-y-auto rounded-2xl border border-slate-200 bg-white/80 p-4">
      {history.length === 0 && (
        <p className="text-sm text-slate-400">La transcription apparaîtra ici…</p>
      )}
      {history.map((m, i) => (
        <div
          key={i}
          className={`max-w-[85%] whitespace-pre-wrap rounded-xl px-3 py-2 text-sm ${
            m.role === "recruiter"
              ? "self-start bg-slate-100 text-slate-800"
              : "self-end bg-brand-600 text-white"
          }`}
        >
          <strong className="mb-0.5 block text-xs font-semibold uppercase tracking-wide opacity-70">
            {m.role === "recruiter" ? "Recruteur" : "Toi"}
          </strong>
          {m.text}
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 3: Type-check**

Run: `npx tsc --noEmit`
Expected: aucune erreur.

- [ ] **Step 4: Commit**

```bash
git add app/components/meeting/RecruiterTile.tsx app/components/meeting/TranscriptPanel.tsx
git commit -m "feat: RecruiterTile (animée) + TranscriptPanel"
```

---

### Task 4: `UserTile` (avatar + webcam locale)

**Files:**
- Create: `app/components/meeting/UserTile.tsx`

**Interfaces:**
- Produces: `UserTile({ cameraOn }: { cameraOn: boolean })` — affiche la webcam locale si `cameraOn`, sinon un avatar ; gère `getUserMedia` en interne, arrête les tracks quand `cameraOn` repasse à false ou au démontage ; en cas de refus/indispo, garde l'avatar + message.

> API navigateur (`getUserMedia`). Vérification : `npx tsc --noEmit`.

- [ ] **Step 1: Write `UserTile`**

Create `app/components/meeting/UserTile.tsx`:

```tsx
"use client";

import { useEffect, useRef, useState } from "react";

export function UserTile({ cameraOn }: { cameraOn: boolean }) {
  const videoRef = useRef<HTMLVideoElement>(null);
  const [error, setError] = useState(false);

  useEffect(() => {
    if (!cameraOn) return;
    let stream: MediaStream | null = null;
    let cancelled = false;
    setError(false);
    navigator.mediaDevices
      ?.getUserMedia({ video: true })
      .then((s) => {
        if (cancelled) {
          s.getTracks().forEach((t) => t.stop());
          return;
        }
        stream = s;
        if (videoRef.current) videoRef.current.srcObject = s;
      })
      .catch(() => setError(true));
    return () => {
      cancelled = true;
      stream?.getTracks().forEach((t) => t.stop());
    };
  }, [cameraOn]);

  return (
    <div className="relative flex aspect-video w-full items-center justify-center overflow-hidden rounded-2xl bg-slate-200">
      {cameraOn && !error ? (
        <video ref={videoRef} autoPlay muted playsInline className="h-full w-full object-cover" />
      ) : (
        <div className="flex flex-col items-center gap-2">
          <span className="grid h-14 w-14 place-items-center rounded-full bg-slate-400 text-lg font-bold text-white">
            Toi
          </span>
          {error && <span className="text-xs text-slate-500">Caméra indisponible</span>}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Type-check**

Run: `npx tsc --noEmit`
Expected: aucune erreur.

- [ ] **Step 3: Commit**

```bash
git add app/components/meeting/UserTile.tsx
git commit -m "feat: UserTile (avatar + webcam locale optionnelle)"
```

---

### Task 5: `MeetingControls` + `MeetingLobby`

**Files:**
- Create: `app/components/meeting/MeetingControls.tsx`
- Create: `app/components/meeting/MeetingLobby.tsx`

**Interfaces:**
- Consumes: `Button` de `@/app/components/ui/Button`.
- Produces:
  - `MeetingControls(props)` avec `props: { muted: boolean; onToggleMute: () => void; showTranscript: boolean; onToggleTranscript: () => void; cameraOn: boolean; onToggleCamera: () => void; currentAnswer: string; onAnswerChange: (v: string) => void; onSend: () => void; onFinish: () => void; streaming: boolean; speechSupported: boolean }`.
  - `MeetingLobby({ onJoin }: { onJoin: () => void })`.

> Vérification : `npx tsc --noEmit`.

- [ ] **Step 1: Write `MeetingControls`**

Create `app/components/meeting/MeetingControls.tsx`:

```tsx
"use client";

import { Button } from "@/app/components/ui/Button";

type Props = {
  muted: boolean;
  onToggleMute: () => void;
  showTranscript: boolean;
  onToggleTranscript: () => void;
  cameraOn: boolean;
  onToggleCamera: () => void;
  currentAnswer: string;
  onAnswerChange: (v: string) => void;
  onSend: () => void;
  onFinish: () => void;
  streaming: boolean;
  speechSupported: boolean;
};

const pill = "flex items-center gap-1.5 rounded-full px-3 py-2 text-sm font-medium transition-colors";

export function MeetingControls({
  muted,
  onToggleMute,
  showTranscript,
  onToggleTranscript,
  cameraOn,
  onToggleCamera,
  currentAnswer,
  onAnswerChange,
  onSend,
  onFinish,
  streaming,
  speechSupported,
}: Props) {
  function onKeyDown(e: React.KeyboardEvent<HTMLTextAreaElement>) {
    if (e.key === "Enter" && !e.shiftKey) {
      e.preventDefault();
      onSend();
    }
  }

  return (
    <div className="flex flex-col gap-3">
      <div className="flex flex-wrap items-center justify-center gap-2">
        <button
          type="button"
          onClick={onToggleMute}
          disabled={!speechSupported}
          className={`${pill} ${muted ? "bg-slate-200 text-slate-600" : "bg-brand-50 text-brand-700"} disabled:opacity-40`}
        >
          {muted ? "🔇 Son coupé" : "🔊 Son"}
        </button>
        <button
          type="button"
          onClick={onToggleTranscript}
          className={`${pill} ${showTranscript ? "bg-brand-50 text-brand-700" : "bg-slate-100 text-slate-600"}`}
        >
          💬 Transcription
        </button>
        <button
          type="button"
          onClick={onToggleCamera}
          className={`${pill} ${cameraOn ? "bg-brand-50 text-brand-700" : "bg-slate-100 text-slate-600"}`}
        >
          {cameraOn ? "📷 Caméra active" : "📷 Activer la caméra"}
        </button>
        <button type="button" onClick={onFinish} disabled={streaming} className={`${pill} bg-red-50 text-red-600 disabled:opacity-40`}>
          ☎️ Terminer
        </button>
      </div>
      <div className="flex items-end gap-2">
        <textarea
          className="min-h-[48px] w-full resize-none rounded-xl border border-slate-300 bg-white px-3.5 py-2.5 text-sm outline-none transition-shadow focus:border-brand-600 focus:ring-4 focus:ring-brand-100 disabled:bg-slate-50"
          value={currentAnswer}
          disabled={streaming}
          onChange={(e) => onAnswerChange(e.target.value)}
          onKeyDown={onKeyDown}
          rows={1}
          placeholder="Ta réponse…  (Entrée pour envoyer)"
        />
        <Button onClick={onSend} disabled={streaming || currentAnswer.trim() === ""}>
          Envoyer
        </Button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Write `MeetingLobby`**

Create `app/components/meeting/MeetingLobby.tsx`:

```tsx
"use client";

import { Button } from "@/app/components/ui/Button";

export function MeetingLobby({ onJoin }: { onJoin: () => void }) {
  return (
    <div className="flex flex-col items-center gap-6 rounded-2xl border border-slate-200 bg-white/80 p-10 text-center shadow-soft animate-rise">
      <span className="grid h-20 w-20 place-items-center rounded-full bg-brand-600 text-2xl font-bold text-white shadow-brand">
        RH
      </span>
      <div>
        <h2 className="font-heading text-xl font-bold text-slate-900">Prêt pour ton entretien ?</h2>
        <p className="mt-1 text-sm text-slate-600">
          Le recruteur va te parler. Active le son de ton appareil.
        </p>
      </div>
      <Button size="lg" onClick={onJoin}>
        Rejoindre l&apos;entretien →
      </Button>
    </div>
  );
}
```

- [ ] **Step 3: Type-check**

Run: `npx tsc --noEmit`
Expected: aucune erreur.

- [ ] **Step 4: Commit**

```bash
git add app/components/meeting/MeetingControls.tsx app/components/meeting/MeetingLobby.tsx
git commit -m "feat: MeetingControls + MeetingLobby (geste de join)"
```

---

### Task 6: `MeetingRoom` (assemblage + pilotage de la voix)

**Files:**
- Create: `app/components/meeting/MeetingRoom.tsx`

**Interfaces:**
- Consumes: `useSpeech` (`@/lib/useSpeech`), `nextSpeakableChunk` (`@/lib/speech`), `RecruiterTile`, `UserTile`, `MeetingControls`, `MeetingLobby`, `TranscriptPanel`, `ChatMessage` (`@/lib/types`).
- Produces: `MeetingRoom(props)` avec `props: { history: ChatMessage[]; streaming: boolean; currentAnswer: string; setCurrentAnswer: (v: string) => void; sendAnswer: () => void; finishInterview: () => void; errorMsg: string | null }`.

> API navigateur. Vérification : `npx tsc --noEmit`.

- [ ] **Step 1: Write `MeetingRoom`**

Create `app/components/meeting/MeetingRoom.tsx`:

```tsx
"use client";

import { useEffect, useRef, useState } from "react";
import type { ChatMessage } from "@/lib/types";
import { useSpeech } from "@/lib/useSpeech";
import { nextSpeakableChunk } from "@/lib/speech";
import { RecruiterTile } from "./RecruiterTile";
import { UserTile } from "./UserTile";
import { MeetingControls } from "./MeetingControls";
import { MeetingLobby } from "./MeetingLobby";
import { TranscriptPanel } from "./TranscriptPanel";

type Props = {
  history: ChatMessage[];
  streaming: boolean;
  currentAnswer: string;
  setCurrentAnswer: (v: string) => void;
  sendAnswer: () => void;
  finishInterview: () => void;
  errorMsg: string | null;
};

export function MeetingRoom({
  history,
  streaming,
  currentAnswer,
  setCurrentAnswer,
  sendAnswer,
  finishInterview,
  errorMsg,
}: Props) {
  const [joined, setJoined] = useState(false);
  const [showTranscript, setShowTranscript] = useState(false);
  const [cameraOn, setCameraOn] = useState(false);
  const { supported, speak, cancel, muted, toggleMute, isSpeaking } = useSpeech();
  const spokenRef = useRef<{ index: number; len: number }>({ index: -1, len: 0 });

  // Fait parler le recruteur phrase par phrase, au fil du flux.
  useEffect(() => {
    if (!joined || muted) return;
    let lastIdx = -1;
    for (let i = history.length - 1; i >= 0; i--) {
      if (history[i].role === "recruiter") {
        lastIdx = i;
        break;
      }
    }
    if (lastIdx === -1) return;
    if (spokenRef.current.index !== lastIdx) spokenRef.current = { index: lastIdx, len: 0 };
    const text = history[lastIdx].text;
    let len = spokenRef.current.len;
    let guard = 0;
    while (guard++ < 200) {
      const res = nextSpeakableChunk(text, len);
      if (res.spokenLen === len) break;
      if (res.chunk) speak(res.chunk);
      len = res.spokenLen;
    }
    spokenRef.current = { index: lastIdx, len };
  }, [history, joined, muted, speak]);

  // Nettoyage de la voix au démontage (fin d'entretien).
  useEffect(() => cancel, [cancel]);

  if (!joined) {
    return <MeetingLobby onJoin={() => setJoined(true)} />;
  }

  return (
    <div className="flex flex-col gap-4 animate-rise">
      <div className="grid grid-cols-1 gap-3 sm:grid-cols-3">
        <div className="sm:col-span-2">
          <RecruiterTile speaking={isSpeaking} />
        </div>
        <UserTile cameraOn={cameraOn} />
      </div>

      {!supported && (
        <p className="rounded-lg bg-amber-50 px-3 py-2 text-sm text-amber-700">
          La synthèse vocale n&apos;est pas supportée sur ce navigateur. Ouvre la transcription
          pour lire l&apos;entretien.
        </p>
      )}
      {errorMsg && <p className="rounded-lg bg-red-50 px-3 py-2 text-sm text-red-600">{errorMsg}</p>}

      {showTranscript && <TranscriptPanel history={history} />}

      <MeetingControls
        muted={muted}
        onToggleMute={toggleMute}
        showTranscript={showTranscript}
        onToggleTranscript={() => setShowTranscript((s) => !s)}
        cameraOn={cameraOn}
        onToggleCamera={() => setCameraOn((c) => !c)}
        currentAnswer={currentAnswer}
        onAnswerChange={setCurrentAnswer}
        onSend={sendAnswer}
        onFinish={finishInterview}
        streaming={streaming}
        speechSupported={supported}
      />
    </div>
  );
}
```

- [ ] **Step 2: Type-check**

Run: `npx tsc --noEmit`
Expected: aucune erreur.

- [ ] **Step 3: Commit**

```bash
git add app/components/meeting/MeetingRoom.tsx
git commit -m "feat: MeetingRoom (assemblage visio + pilotage voix phrase par phrase)"
```

---

### Task 7: Intégration dans `app/page.tsx`

**Files:**
- Modify: `app/page.tsx` (bloc `phase === "chat"`)

**Interfaces:**
- Consumes: `MeetingRoom` (`@/app/components/meeting/MeetingRoom`).

> Vérification finale au navigateur (le projet ne teste pas les pages en unitaire).

- [ ] **Step 1: Add the import**

Dans `app/page.tsx`, ajoute près des autres imports :

```tsx
import { MeetingRoom } from "@/app/components/meeting/MeetingRoom";
```

- [ ] **Step 2: Replace the `phase === "chat"` block**

Repère le bloc `{phase === "chat" && ( … )}` (la `div` avec la boîte de conversation scrollable, la zone d'erreur et la `Card` d'input). Remplace **tout ce bloc** par :

```tsx
      {phase === "chat" && (
        <MeetingRoom
          history={history}
          streaming={streaming}
          currentAnswer={currentAnswer}
          setCurrentAnswer={setCurrentAnswer}
          sendAnswer={sendAnswer}
          finishInterview={finishInterview}
          errorMsg={errorMsg}
        />
      )}
```

Le `ref` d'auto-scroll `chatScrollRef` et son `useEffect` ne servent plus qu'à l'ancien chat : s'ils ne sont plus référencés après ce remplacement, supprime la déclaration `const chatScrollRef = useRef…` et le `useEffect` d'auto-scroll associé pour éviter le code mort et les warnings lint. Ne touche à rien d'autre (form, debrief, streamRecruiter, finishInterview).

- [ ] **Step 3: Type-check and run the unit suite**

Run: `npx tsc --noEmit && npx vitest run`
Expected: aucune erreur de type ; tous les tests passent (dont `tests/speech.test.ts`).

- [ ] **Step 4: Verify in the browser**

Run: `npm run dev`, ouvrir `http://localhost:3000`. Remplir le formulaire (poste + CV, ou un template si présent), « Démarrer l'entretien ». Vérifier :
1. La phase entretien montre le **salon** « Prêt ? [Rejoindre l'entretien] ».
2. Clic « Rejoindre » → la salle visio s'affiche (tuile recruteur + tuile « Toi »), et le recruteur **parle** (son activé) ; sa tuile s'anime pendant qu'il parle.
3. « Son coupé » stoppe la voix ; « Transcription » affiche/masque les bulles ; « Activer la caméra » demande l'accès et affiche la webcam (ou « Caméra indisponible » si refus).
4. Répondre au clavier (Entrée) → le recruteur répond et parle.
5. « Terminer » → génération du débrief (inchangé).

- [ ] **Step 5: Commit**

```bash
git add app/page.tsx
git commit -m "feat: phase entretien en mode réunion vocal"
```

---

## Notes d'exécution

- Ordre : Task 1 → 7. Tasks 1-2 (logique/hook) et 3-5 (composants) sont indépendantes ; Task 6 dépend de 1-5 ; Task 7 dépend de 6.
- Après Task 7, vérification end-to-end au navigateur (salon → voix → contrôles → débrief).
- `nextSpeakableChunk` est le seul morceau testé en unitaire ; tout le reste (API navigateur) est vérifié au navigateur, conformément au projet.
